module.exports = {
  // Options that will be passed to `eslint-config-frontful/provider`
  // that generates ESLint configuration
  options: null,
  // Raw ESLint configuration that can be created manually
  // or using `eslint-config-frontful/provider`
  config: null,
}
